\chapter{Inapprossimabilità}

% \section{Inapprossimabilità di Max-Clique}

\begin{theorem}[\cite{FGLSS96}] \label{thm:max-clique}
	Per ogni $\varepsilon > 0$, non esiste una $\varepsilon$-approssimazione
	polinomiale di \texttt{MAX-CLIQUE}, a meno che $\textbf{P} = \textbf{NP}$.
\end{theorem}

\begin{proof}
	TBD.
\end{proof}

\begin{theorem}
	Esiste una costante $c > 0$ tale che non esista una
	$\frac1{n^c}$-approssimazione polinomiale di \texttt{MAX-CLIQUE}, a meno
	che $\textbf{P} = \textbf{NP}$.
\end{theorem}

\begin{proof}
	TBD.
\end{proof}

\begin{theorem}
	Esiste un $\varepsilon_1 > 0$ tale che non esiste una
	$\varepsilon_1$-approssimazione polinomiale di \texttt{MAX-3SAT}, a meno che
	$\textbf{P} = \textbf{NP}$.
\end{theorem}

\begin{proof}
	TBD.
\end{proof}
