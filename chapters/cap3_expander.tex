\chapter{Grafi expander} \label{chap:expander}

% mi piacerebbe mettere una citazione all'inizio di ogni capitolo
% "the hay in the haystack"?

In questo capitolo introduciamo i grafi expander e le loro proprietà
pseudo-casuali. Ci occuperemo, di grafi non diretti e regolari, cosicché la
matrice di transizione delle passeggiata aleatoria sul grafo sia simmetrica.
Per il teorema spettrale, è possibile dare una caratterizzazione
I grafi expander saranno un ingrediente fondamentale nel
capitolo \ref{chap:pcpproof}.

\section{Cos'è un expander}

Un grafo si dice regolare di grado $d$, o $d$-regolare, se ogni nodo ha
esattamente $d$ vicini. Sia dunque $G$ un grafo $d$-regolare e $A$ la sua
matrice di adiacenza rinormalizzata, cioè la matrice di transizione delle
passeggiate aleatorie su $G$.
La matrice $A$ è simmetrica, dunque per il teorema spettrale esiste una
base ortogonale di autovettori.
Definiamo il parametro

\begin{definition}[Grafi expander]
	Fissato $\lambda < 1$, chiamiamo \emph{$(n, d, \lambda)$-expander} un grafo
	di $n$ nodi, regolare di grado $d$ e con $\lambda(G) \le \lambda$.
\end{definition}

\begin{definition}[Famiglie di expander]
	Una famiglia di grafi $\{G_n\}_{n \in \NN}$ si dice \emph{famiglia di
	$(n, d)$-expander} se esistono costanti $d \in \NN$ e $\lambda < 1$ tali
	che per ogni $n$ il grafo $G_n$ è un $(n, d, \lambda)$-expander.
\end{definition}

\section{Esistenza di expander}

Un grafo generato tramite un opportuno processo random è un expander con
buona probabilità.

\section{Proprietà}

\begin{theorem}[Passeggiate aleatorie su expander] \label{thm:expander-walk}
	Sia $G$ un $(n, d, \lambda)$-expander e sia $B \subseteq V$ un sottoinsieme
	dei vertici che soddisfi $|B| \le \beta n$ per $\beta \in (0, 1)$.
	Detti $X_1, \dots, X_k$ i nodi di una passeggiata aleatoria su $G$ con $X_1$
	scelto uniformemente in $V$, vale
	\[
		\Pr[\forall 1 \le i \le k \ X_i \in B]
		\le ((1 - \lambda)\sqrt{\beta} + \lambda)^{k - 1}.
	\]
\end{theorem}
\begin{proof}
	TBD.
\end{proof}

Il Teorema \ref{thm:expander-walk} sarà di fondamentale importanza per
``riciclare bit random'', come nella dimostrazione del Corollario
\ref{thm:maxclique-inapprox-nc}.


\begin{restatable}{lemma}{expanderFwalk} \label{thm:Din06Prop2.5}
	% serve without self-loops? No, purché un cappio abbia contributo 2 al grado
	Sia $G = (V, E)$ un $(d, \lambda)$-expander e $F \subseteq E$ un sottoinsieme
	degli archi. Sia $K$ la distribuzione di probabilità sui vertici indotta
	dalla scelta di un estremo di un arco in $F$ scelto uniformemente a caso.
	La probabilità $p$ che una passeggiata aleatoria che comincia con
	distribuzione $K$ faccia l'$(i+1)$-esimo passo in $F$ è maggiorata da
	$\frac{|F|}{|E|} + \lambda^i$.
\end{restatable}

\begin{proof} % [Dimostrazione del Lemma \ref{thm:Din06Prop2.5}]
	Siano $x$ il vettore distribuzione di $K$, cioè	$x_v = \frac{\deg_F(v)}
	{2 |F|}$, e $y_v$ la probabilità che un arco incidente a $v$ sia in $F$, cioè
	$y = \frac{2 |F|}{d} x$.
	Scomponiamo $x = x^\parallel + x^\perp$ con
	$x^\parallel := \frac1n \mathbf{1}$. Vale per definizione
	\[
		p = \sum_{v \in V}{ y_v (A^i x)_v }
		  = \langle y, A^i x \rangle.
	\]

	Osserviamo $\lVert x \rVert ^2 \le \big(\sum_v{\lvert x_v \rvert} \big)
	\max_v{\lvert x_v \rvert} = \max_v{\lvert x_v \rvert}
	\le \frac{d}{2 | F |}$ da cui, applicando la disuguaglianza di Cauchy-Schwarz
	e la definizione variazionale di $\lambda$, otteniamo
	\[
		\big\langle y, A^i x^\perp \big\rangle
		\le \lVert y \rVert \cdot \lVert A^i x^\perp \rVert
		\le \frac{2 |F|}{d} \lVert x \rVert \cdot \lambda^i \lVert x \rVert
		\le \lambda^i.
	\]

	In conclusione,
	\[
		\langle y, A^i x \rangle
		= \big\langle y, A^i x^\parallel \big\rangle
		+ \big\langle y, A^i x^\perp \big\rangle
		\le \frac{2 |F|}{n d} + \lambda^i.
	\]
\end{proof}

\iffalse
\begin{lemma}[Expander mixing lemma] \label{thm:mixing-lemma}
	Sia $G = (V, E)$ un grafo $(d, \lambda)$-expander e siano $S, T \subseteq V$
	due sottoinsiemi disgiunti di nodi. Allora, detto $E(S, T)$ il numero di
	archi tra $S$ e $T$, vale
	\[
		\Big| E(S, T) - \frac{d}{n} |S||T| \Big| \le \lambda \sqrt{|S| |T|}.
	\]
\end{lemma}
\begin{proof}
	% Detta $A$ la matrice di adiacenza rinormalizzata di $G$, considera $B = A - \frac{d}{n}J$, con $J$ la matrice uno in ogni entrata.

	TBD.
\end{proof}

A volte è utile specializzare il mixing lemma per stimare il numero di archi
tra un insieme e il suo complementare, come segue.
\fi

\begin{lemma} \label{thm:ESbarS}
	Sia $G = (V, E)$ un $(n, d, \lambda)$-expander e $S \subseteq V$ un
	sottoinsieme dei vertici. Allora
	\[
		E(S, \bar{S}) \ge (1 - \lambda)\frac{d}{n}|S||\bar{S}|.
	\]
\end{lemma}
\begin{proof}
	Esprimiamo $E(S, \bar S)$ come un opportuno prodotto scalare e analizziamo
	separatamente componente parallela e perpendicolare al vettore costante.
	Detta $A$ la matrice di adiacenza rinormalizzata, siano $\mathbf{1}_S$ e
	$\mathbf{1}_{\bar{S}}$ i vettori indicatori di $S$ e del suo complementare.
	Siano anche $\mathbf{1}_S^\parallel = \frac{|S|}{n} \mathbf{1}$ e
	$\mathbf{1}_S^\perp = \mathbf{1}_S - \mathbf{1}_S^\parallel$ le componenti
	di $\mathbf{1}_S$ parallela e perpendicolare a $\mathbf{1}$.
	Vale
	\begin{align*}
		\frac{1}{d} E(S, \bar{S}) &= \langle \mathbf{1}_S, A \mathbf{1}_{\bar S} \rangle \\
		&= \langle \mathbf{1}_S, A (\mathbf{1} - \mathbf{1}_{\bar S}) \rangle \\
		&= \langle \mathbf{1}_S, \mathbf{1} \rangle
			- \langle \mathbf{1}_S, A \mathbf{1}_S \rangle \\
		&= |S| - \big\langle \mathbf{1}_S^\parallel + \mathbf{1}_S^\perp,
			A (\mathbf{1}_S^\parallel + \mathbf{1}_S^\perp ) \big\rangle \\
		&= |S| - \big\langle \mathbf{1}_S^\parallel, \mathbf{1}_S^\parallel \big\rangle
			- \big\langle \mathbf{1}_S^\perp, A \mathbf{1}_S^\perp \big\rangle
			- 2 \big\langle \mathbf{1}_S^\parallel, \mathbf{1}_S^\perp \big\rangle \\
		&\ge |S| - \big\langle \mathbf{1}_S^\parallel, \mathbf{1}_S^\parallel \big\rangle
			- \lambda \big\langle \mathbf{1}_S^\perp, \mathbf{1}_S^\perp \big\rangle \\
		&= |S| - \frac{|S|^2}{n} - \lambda\bigg(|S| - \frac{|S|^2}{n}\bigg) \\
		&= (1 - \lambda)\frac{|S|}{n}(n - |S|) = (1 - \lambda) \frac1{n} |S| |\bar S|.
	\end{align*}
\end{proof}

% Significato dell'expander mixing lemma, quello è proprio il valore atteso di archi in un grafo random.

\section{Prodotto zig-zag}

\begin{theorem} \label{thm:expander-construction}
	Esiste una famiglia fortemente esplicita di $(\lambda, d)$-expander per
	qualche $d \in \mathbb{N}$ e $\lambda < 1$.
\end{theorem}
\begin{proof}
	TBD.
\end{proof}

Notiamo che grazie all'elevamento a potenza di grafi il Teorema
\ref{thm:expander-construction} fornisce famiglie fortemente esplicite di
$\lambda$-expander per ogni $\lambda > 0$ al prezzo di un aumento del grado $d$.

\begin{corollary} \label{thm:maxclique-inapprox-nc}
	Esiste una costante $c > 0$ tale che non esiste una
	$\frac1{n^c}$-approssimazione polinomiale di \texttt{MAX-CLIQUE}, a meno
	che $\textbf{P} = \textbf{NP}$.
\end{corollary}

\begin{proof}
	TBD.

	In sostanza, la dimostrazione è identica alla dimostrazione
	dell'inapprossimabilità di \texttt{MAX-CLIQUE} a meno di costante,
	però ripeto la verifica $O(\log n)$ volte riciclando i bit random.
\end{proof}
